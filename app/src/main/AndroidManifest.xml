<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.my.pucmap">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zzz">
        <activity android:name=".Get_Me_Somewhere_2"></activity>
        <activity android:name=".Map_Direction" />
        <activity android:name=".Fra_Pu" />
        <activity android:name=".Ve_Pu" />
        <activity android:name=".Zd_Pu" />
        <activity android:name=".Pu_Ve" />
        <activity android:name=".Pu_Fra" />
        <activity android:name=".Pu_Brij" />
        <activity android:name=".Pu_Zd" />
        <activity android:name=".Verude_Kol" />
        <activity android:name=".Stoja_kol" />
        <activity android:name=".Kol_Verudela" />
        <activity android:name=".Kol_stoja" />
        <activity android:name=".Get_Me_Somewhere" />
        <activity android:name=".Get_Me_Work" />
        <activity android:name=".Get_Me_Home" />
        <activity android:name=".Slots" />
        <activity android:name=".Third_zone" />
        <activity android:name=".Second_zone" />
        <activity android:name=".First_zone" />
        <activity android:name=".SMS_parking" />
        <activity android:name=".Boat" />
        <activity android:name=".Taxi" />
        <activity android:name=".Parking" />
        <activity android:name=".Bus" />
        <activity android:name=".Bike" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>